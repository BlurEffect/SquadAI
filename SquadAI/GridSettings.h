/* 
*  Kevin Meergans, SquadAI, 2014
*  GridSettings.h
*  Contains some constants defining the properties of the grid drawable.
*/

#ifndef GRID_SETTINGS_H
#define GRID_SETTINGS_H

const float g_cGridWidth				    = 100.0f; // The extents of the grid along the x-axis
const float g_cGridHeight				    = 100.0f; // The extents of the grid along the y-axis
const int   g_cNumberOfHorizontalPartitions = 10;	  // The number of grid fields along the x-axis
const int   g_cNumberOfVerticalPartitions   = 10;	  // The number of grid fields along the y-axis

#endif // GRID_SETTINGS_H