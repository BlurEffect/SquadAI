/* 
*  Kevin Meergans, SquadAI, 2014
*  EntityMemory.h
*  This class represents the memory of the entity and bundles all
*  information the entity uses to inform its decisions.
*/

#ifndef ENTITY_MEMORY_H
#define ENTITY_MEMORY_H

class EntityMemory
{
public:
	EntityMemory(void);
	~EntityMemory(void);

private:

};

#endif // ENTITY_MEMORY_H